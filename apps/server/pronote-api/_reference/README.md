# Legacy pronote-api reference

Snapshot of the pre-rewrite client (Merlode11-based), kept for comparison when debugging auth or fetch behaviour.

The active implementation lives in `../src/`. Auth and SSO were rewritten from `docs/agents/connexion.har` (EduConnect → CAS Haute-Garonne → PRONOTE).
